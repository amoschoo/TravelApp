<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amos.travelapp" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!--
 The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".Resources.GlobalValue"
        android:allowBackup="true"
        android:icon="@mipmap/bootsicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Pages.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Pages.RouteDetails" >
        </activity>
        <activity android:name=".Pages.Budget" >
        </activity>
        <activity android:name=".Pages.Currency" >
        </activity>
        <activity android:name=".Pages.Weather" >
        </activity>
        <activity android:name=".Pages.Attractions" >
        </activity>
        <activity android:name=".Pages.Language" >
        </activity>
        <activity android:name=".Pages.attraction_MBS" >
        </activity>
        <activity android:name=".Pages.Attractions_MBS" >
        </activity>
        <activity android:name=".Pages.Attractions_SF" >
        </activity>
        <activity android:name=".Pages.Attractions_VC" >
        </activity>
        <activity android:name=".Pages.Attractions_RWS" >
        </activity>
        <activity android:name=".Pages.Attractions_BTRT" >
        </activity>
        <activity android:name=".Pages.Attractions_ZOO" >
        </activity>
    </application>

</manifest>
